// Constants
// const SOMETHING_CHANGED = 'cards/game/something_changed'
export const constants = {
}
// Action Creators
export const actions = {
}

// Reducer
export const defaultState = {
  phase: 'WaitingToStart'
}

export default function (state = defaultState, action) {
  switch (action.type) {
    default:
      return state
  }
}
