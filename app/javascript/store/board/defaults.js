import {regionDefaults} from 'store/card/defaults'

export const initialBoard = {
  city: regionDefaults.city,
  frontier: regionDefaults.frontier,
  border: regionDefaults.border,
  wilderness: regionDefaults.wilderness,
  lair: regionDefaults.lair
}

export default initialBoard
